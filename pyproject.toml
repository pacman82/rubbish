[project]
name = "rubbish"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "torch>=2.9.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
